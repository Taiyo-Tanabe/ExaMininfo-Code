name: Keep Alive

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend
        run: curl -sf "${{ secrets.BACKEND_URL }}/schools/?limit=1" > /dev/null && echo "OK" || echo "Failed"
